{
  "name": "json-server",
  "version": "1.0.0",
  "description": "how to use json-server",
  "main": "main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "nodemon main.js",
    "start": "npm run gen-data && node main.js",
    "gen-data": "node generate-data.js"
  },
  "keywords": [
    "json-server"
  ],
  "author": "Tim",
  "license": "ISC",
  "dependencies": {
    "json-server": "^0.17.0",
    "query-string": "^7.1.1"
  },
  "devDependencies": {
    "faker": "^5.5.3",
    "nodemon": "^2.0.15"
  }
}
